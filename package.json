{
  "name": "chapter_4",
  "version": "1.0.0",
  "description": "A dockerized full stack todo application that uses a NodeJS backend, a PostgreSQL database, a Primsa ORM, and JWT authentication",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "nodemon --watch src --ext js,json --exec \"node --env-file=.env --experimental-strip-types --experimental-sqlite ./src/server.js\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/adapter-pg": "^6.2.1",
    "@prisma/client": "^7.1.0",
    "bcryptjs": "^3.0.3",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.16.3",
    "prisma": "^7.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.11"
  }
}
